import React from "react";
import styles from "./ReadReviewCell.module.scss";

const ReadReviewCell: React.FunctionComponent<{}> = (props: {}) => {
  return <div className={styles.ReadReviewCell}>read more</div>;
};

export default ReadReviewCell;
